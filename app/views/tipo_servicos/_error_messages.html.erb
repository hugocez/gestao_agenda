<% if @tiposervico.errors.any? %>
	<div id="error_explanation">
		<div class="alert alert-error">
			The form contains <%= pluralize(@tiposervico.errors.count, "error") %>.
		</div>
		<ul>
			<% @tiposervico.errors.full_messages.each do |msg| %>
				<li>* <%= msg %></li>
			<% end %>
		</ul>
	</div>
<% end %>
  