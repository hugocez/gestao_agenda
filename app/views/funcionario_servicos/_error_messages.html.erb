<% if @funcionarioServico.errors.any? %>
	<div id="error_explanation">
		<div class="alert alert-error">
			The form contains <%= pluralize(@funcionarioServico.errors.count, "error") %>.
		</div>
		<ul>
			<% @funcionarioServico.errors.full_messages.each do |msg| %>
				<li>* <%= msg %></li>
			<% end %>
		</ul>
	</div>
<% end %>
  