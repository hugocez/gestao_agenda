		<tr>
		  	<td>
		  		<%if funcionario.cpf? %>
		  			<%= link_to funcionario.cpf, edit_funcionario_path(funcionario.id) %>
		  		<%end%>	
		  	</td>
	  		<td><%= link_to funcionario.nome, edit_funcionario_path(funcionario.id) %></td>
  			<td><% if funcionario.user != nil %> 
  					<%= link_to funcionario.user.email, user_path(funcionario.user.id) %>
  				<%else%>
  					<%= link_to "Cadastrar", edit_funcionario_path(funcionario.id) %>
  				<%end%>
  			</td>
  			<td>		<%= link_to "Excluir", funcionario, method: :delete,
                                  data: { confirm: "Favor confirmar a exclusão de " + funcionario.nome } %> - <%= link_to "Consultar", funcionario%>
        	</td>
		</tr>

